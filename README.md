Built a scraper at scrape_videos.py that pulls all videos from the influencer's Amazon storefront, including title, product link, and whether each video is currently shown on its product page. Run it with python3 scrape_videos.py. (disable recaps in /config)